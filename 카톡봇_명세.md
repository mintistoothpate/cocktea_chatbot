# 칵테일 동아리 카톡 봇 명세서

## 목적
- 동아리방의 재고를 더 쉽게 정리할 수 있도록 한다.
- 부수적으로 칵테일 추천 같은 기능도 함께 제공한다.

## 배경
칵테일 동아리 특성상 재고가 쉽게 바뀌고, 이를 추적하기 위한 규칙이 번거롭다. 따라서 아래와 같은 동작을 수행하는 카톡 봇을 만들어 동아리방 사용 및 접근성을 높이고자 한다.

## 플랫폼
- **카카오 i 오픈빌더** 기반으로 구현 예정.
- 능동 발화(스케줄링된 알림 등)에 제약이 있으므로, 모든 상태 갱신은 사용자가 명령어를 입력하는 시점에 처리한다 (lazy 방식).

## 핵심 설계 철학 (중요)

이 봇은 **재고의 정확한 양을 추적하는 시스템이 아니라, 재고가 부족할 가능성을 알리는 시스템**이다.

- **`!사용`이 차감하는 양은 추정치**이다. 이유:
  - 1 oz는 약 30 mL인데 정확히 30 mL은 아니다.
  - 부원들이 현실적으로 1 oz를 정확히 따를 수 없다.
- 따라서 DB 상의 남은 양과 실제 남은 양 사이에는 항상 오차가 존재한다.
- 이 오차를 인정하고, **상태(이상없음/위험/부족)로 부족 가능성만 알리는 방식**으로 운영한다.
- **실제로 다 썼는지는 사람이 눈으로 확인한 후 `!소진` 명령어로 확정**한다.

이 철학에서 도출되는 규칙:
- `!사용`은 절대 병 개수를 건드리지 않는다 (총량만 차감).
- `!사용`으로 총량이 0이 되어도 자동으로 `소진` 처리하지 않는다. `부족` 상태로 두고 사용자가 직접 확인하게 한다.
- 병 정리(병 개수 차감)는 오직 `!소진`으로만 이루어진다.

---

## 단위 규칙 (중요)

- **내부 저장 단위**: 모든 재고 DB의 양은 `oz` (1 oz = 30 mL) 기준으로 저장한다.
- **입력 단위**: 명령어마다 사용자 친화적인 단위를 사용하며, 봇이 내부적으로 oz로 환산해서 저장/차감한다.

| 명령어 | 입력 단위 | 환산 처리 |
|--------|----------|-----------|
| `!사용 재료` | oz (기본), 양이 많을 경우 mL | mL이면 oz로 환산해서 차감 |
| `!사용 레시피` | 잔 수 (정수) | 레시피 DB 참조, 재료별 양 × 잔 수로 자동 차감 |
| `!정기활동` | 잔 수 (정수) | 레시피 DB 참조, 재료별 양 × 잔 수로 자동 차감 |
| `!추가` | mL (시중 음료가 mL 표기이므로) | oz로 환산해서 DB에 저장 |

---

## 0. 재고 DB 구조

재고 정보는 두 개의 테이블로 분리해서 관리한다.

### 0-1. 메인 재고 DB (재료 단위 요약)

사용자가 자주 조회하는 정보. 재료마다 1행.

| 물품 이름 | 병 개수 | 남은 양(oz) | 상태 |
|----------|--------|------------|------|

- **병 개수**: 현재 보유 중인 병의 총 수.
- **남은 양(oz)**: 모든 병의 양을 합산한 총량.
- **상태**: 총량을 기준으로 자동 판정 (아래 상태 분류 기준 참고).

### 0-2. 병 상세 DB (병 단위 상세)

각 병의 개별 정보. 한 재료에 병이 여러 개면 여러 행.

| 물품 이름 | 병 번호 | 용량(oz) | 소비기한 |
|----------|--------|---------|---------|

- **병 번호**: 같은 재료 안에서 등록 순서대로 부여 (1, 2, 3...).
- **용량(oz)**: 해당 병에 처음 들어 있던 양 (mL 입력 → oz 환산).
- **소비기한**: `YYYYMMDD` 또는 `null`.

### 동작 규칙

- **`!추가`**: 병 상세 DB에 새 행 추가, 메인 재고 DB의 병 개수와 남은 양 갱신.
- **`!사용`**: 메인 재고 DB의 남은 양에서 차감. 병 상세 DB는 건드리지 않음.
- **`!소진`**:
  - **임의로 먼저 들어온 병(병 번호가 가장 작은 행)을 병 상세 DB에서 제거**.
  - 메인 재고 DB의 병 개수를 1 차감.
  - 남은 양은 `null`로, 상태는 `소진`으로 변경 (병 개수가 0이 되었을 때).
  - ※ 병 개수가 아직 남았는데 한 병을 다 쓴 경우의 처리도 동일 (한 병만 제거하고 개수만 1 차감).
- **상태 자동 판정**: 메인 재고 DB의 남은 양(총량)에 따라 상태가 결정된다.
- **만료 판정** (`!추천` / `!재고` 실행 시):
  - 병 상세 DB를 훑어 소비기한 지난 병이 있으면, 해당 재료의 메인 재고 상태를 `만료`로 갱신.

### 상태 분류 기준

| 상태 | 기준 | 의미 |
|------|------|------|
| **이상없음** | 총량 5 oz 초과 | 재료 양이 충분해서 무엇이든 만들 수 있는 상태 |
| **위험** | 총량 0 oz 초과 ~ 5 oz 이하 | 살짝 부족한 상태 |
| **부족** | 총량 0 oz 이하 | 사용 로그상 이미 다 소진했지만, 각자의 oz마다 차이가 있어 약간 남아 있거나 없을 수 있는 상태 |
| **소진** | 병 개수 0 (`!소진` 명령어로 확인) | 명령어로 다 쓴 것을 확인한 상태 |
| **만료** | 병 중 하나라도 소비기한 경과 | 소비기한이 지나서 사용할 수 없는 상태 |

> ※ 상태 우선순위: **만료** > **소진** > (양에 따른) 부족/위험/이상없음.

### 만료 판정 시점 (lazy 체크)
- 카카오 i 오픈빌더의 능동 발화 제약상 주기적 자동 체크는 어렵다.
- 따라서 **`!추천` 및 `!재고` 명령어가 실행될 때**, 봇이 병 상세 DB를 훑어 소비기한이 지난 병이 있는 재료의 상태를 `만료`로 갱신한다.
- 다른 명령어(`!사용`, `!소진`, `!추가`)에서는 만료 체크를 별도로 수행하지 않는다.

---

## 1. `!사용` 명령어 (일반 사용자)

### 사용법
```
!사용 재료 [재료] [사용한 양], [재료] [사용한 양], ...
!사용 레시피 [레시피] [제작 수량], [레시피] [제작 수량], ...
```

### 규칙
- **재료/레시피 명시 구분**: 첫 번째 인자로 `재료` 또는 `레시피`를 명시한다 (재료명과 레시피명이 겹치는 경우를 방지하기 위함).
- **단위 기준 (재료)**: 사용한 양은 기본적으로 `oz` 기준으로 작성한다. 양이 많을 경우 `mL`로 적었는지 확인하고, mL이면 oz로 환산해서 처리한다.
- **레시피 단위 처리**:
  - `!사용 레시피 [레시피] [제작 수량]`을 입력하면 봇이 레시피 DB에서 해당 레시피의 재료 구성을 조회한다.
  - 각 재료의 양에 제작 수량을 곱해서 메인 재고 DB에서 자동 차감한다.
  - 예: 모히토 레시피가 "럼 2oz, 라임주스 1oz, 시럽 0.5oz"이고 `!사용 레시피 모히토 2`를 입력하면 럼 4oz, 라임주스 2oz, 시럽 1oz를 차감.
- **DB 검증**: 재료/레시피가 DB에 없으면 오타인지 확인하도록 안내한다.
- **자동 갱신**: DB의 남은 양을 실시간으로 계산해서 차감한다.
- **상태 자동 갱신**: 차감 후 남은 양에 따라 상태(이상없음/위험/부족)를 자동으로 업데이트한다.
- **동방 인원 자동 추가**: `!사용`을 입력한 사람은 자동으로 현재 동방 사용 인원에 추가된다 (`!입장` 효과 겸함).

---

## 2. `!소진` 명령어 (일반 사용자)

### 사용법
```
!소진 [재료], [재료], [재료], ...
```

### 규칙
- 다 쓴 재료를 확인하기 위함.
- 여러 재료를 한 번에 소진 처리할 수 있다.
- 각 재료에 대해:
  - 상태를 `소진`으로 바꾼다.
  - 남은 양을 `null`로 바꾼다. (정렬·분류 시 정상 재고와 명확히 구분하기 위함)
- **null 사용 이유**: oz 단위로 기록하다 보니 `-1` 같은 음수도 실제 값으로 나올 수 있어 구분이 어렵다. `null`은 의미상으로도 "값 없음"이 직관적이다.

---

## 3. `!추천` 명령어 (일반 사용자)

### 사용법
```
!추천
```

### 규칙
- 남아 있는 재고와 레시피 DB를 비교해서 만들 수 있는 칵테일을 추천한다.
- 각 레시피의 도수, 맛 (단맛, 알싸함, 신맛 등), 특징을 기준으로 추천한다.

### 재고 상태별 추천 로직
| 레시피에 들어간 재료의 상태 | 추천 처리 |
|------------------------|----------|
| 이상없음만 사용 | 정상 추천 |
| 위험 / 부족 포함 | 추천하되, **"재료가 부족할 수 있음"** 알림 표시 |
| 소진 포함 | 추천에서 **제외** |
| 만료 포함 | 추천에서 **제외** |

---

## 4. `!추가` 명령어 (⚠️ 관리자 전용)

### 사용법
```
!추가 [재료] [양] [소비기한], [재료] [양] [소비기한], ...
```

### 규칙
- **관리자 전용**: 평소 동아리 사용자는 사용하지 않으며, 관리자가 재고를 보충할 때 사용한다.
- **목적**: 한 번에 여러 재료를 추가하고, 추가된 재료를 동아리원이 눈으로 확인할 수 있도록 한다.
- **단위 기준**: 시중에 파는 음료가 `mL` 단위로 표기되어 있으므로 양은 `mL`로 작성한다.
- **내부 처리**: 봇이 입력된 mL을 `oz`로 환산해서 DB에 저장한다.
- **소비기한 포맷**: 8자리 숫자 (`YYYYMMDD`). 예: `20260815`.
- **소비기한이 없는 재료** (보드카, 진 등 증류주):
  - 입력 시 `-` 또는 `없음`을 적으면 봇이 DB에 `null`로 저장한다.
  - `null`인 재료는 `!추천` / `!재고` 실행 시 만료 체크에서 제외된다.
- **DB에 없는 재료 처리**:
  - 입력된 재료 이름이 DB에 없으면 봇이 "**[재료명]은(는) 재고 목록에 없는 재료입니다. 오타 없이 정확히 입력하신 것이 맞나요?**" 같은 확인 메시지를 보낸다.
  - 관리자가 확인하면 새 항목으로 DB에 등록한다.
  - 오타였으면 다시 입력하도록 한다.

---

## 5. `!재고` 명령어 (⚠️ 관리자 전용)

### 사용법
```
!재고                  → 이상 있는 재료 전체 조회
!재고 [상태]            → 특정 상태에 해당하는 재료 조회 (예: !재고 위험)
!재고 [재료 이름]        → 특정 재료의 현재 상태/남은 양 조회
```

### 규칙
- **관리자 전용**.

#### (1) 인자 없이 `!재고`
- `이상없음`이 아닌 모든 재료를 상태별로 그룹화해서 보여준다.
  - 만료: [재료 목록]
  - 소진: [재료 목록]
  - 부족: [재료 목록]
  - 위험: [재료 목록]
- 이상이 있는 재료가 없으면 "현재 이상 없는 재고만 있음" 같은 메시지 출력.

#### (2) `!재고 [상태]`
- 해당 상태(`이상없음` / `위험` / `부족` / `소진` / `만료`)에 속하는 재료들을 출력.

#### (3) `!재고 [재료 이름]`
- 해당 재료의 현재 남은 양, 상태, 소비기한을 출력.
- DB에 없으면 오타 확인 메시지.

#### 출력 개수 제한
- 한 번에 출력되는 재료가 **20개를 초과**하면, 전체를 출력하지 않고 **"결과가 너무 많습니다 (N개). 조건을 좁혀 다시 검색해 주세요."** 같은 오류 메시지를 반환한다.
- 이상없음 재료가 많을 때 `!재고 이상없음`이 무한정 길어지는 것을 방지하기 위함.

#### 만료 상태 갱신 (lazy 체크)
- `!재고` 실행 시 봇은 DB를 훑어 소비기한이 지난 재료를 `만료` 상태로 갱신한 후 조회한다.

---

## 6. `!취소` 명령어 (일반 사용자)

### 사용법
```
!취소
```

### 규칙
- **본인이 마지막으로 친 명령**을 되돌린다.
- 카톡 봇 특성상 오타 한 번이 DB를 망칠 수 있어 안전장치로 둔다.
- **대상 명령어**: `!사용`, `!소진`, `!추가` (DB를 변경하는 명령어).
- **취소 가능 범위**:
  - 사용자별로 **가장 마지막 명령 1회**만 취소 가능 (연속 취소 불가).
  - 명령 실행 직후에만 취소 가능 (시간 제한은 추후 결정 — 예: 1시간 이내).
- **취소 처리 방식**:
  - `!사용` 취소 → 차감한 양만큼 메인 재고 DB에 다시 더해줌.
  - `!소진` 취소 → 제거한 병을 병 상세 DB에 복원, 메인 재고 DB의 병 개수 복원.
  - `!추가` 취소 → 추가한 병을 병 상세 DB에서 제거, 메인 재고 DB의 병 개수와 양 차감.
- **로그 필요**: 취소를 구현하려면 최근 명령 히스토리를 저장해야 함.

---

## 7. `!도움말` 명령어 (일반 사용자)

### 사용법
```
!도움말
!도움말 [명령어]
```

### 규칙
- **인자 없이 `!도움말`**: 사용 가능한 모든 명령어 목록과 한 줄 설명을 출력.
- **`!도움말 [명령어]`**: 특정 명령어의 상세 사용법과 예시를 출력 (예: `!도움말 사용`, `!도움말 추가`).
- 동아리에 새로 가입한 부원이 봇 사용법을 빠르게 익힐 수 있도록 한다.
- 관리자 전용 명령어(`!추가`, `!재고`, `!동방인원수정`)는 출력 시 `[관리자 전용]` 표시를 함께 보여준다.

---

## 8. `!재료` 명령어 (일반 사용자)

### 사용법
```
!재료 [물품 이름]
```

### 규칙
- 특정 재료의 현재 상태를 빠르게 확인한다.
- 출력 예시: "보드카: 충분합니다 (이상없음)" / "라임주스: 부족할 수 있습니다 (위험)" / "진: 부족합니다 (부족)" / "트리플섹: 소진되었습니다 (소진)" / "우유: 만료되었습니다 (만료)"
- DB에 없는 재료면 오타 확인 메시지.
- 만료 체크 lazy 갱신: 실행 시 해당 재료의 병 상세 DB를 훑어 소비기한 검사.

---

## 9. `!제작가능` 명령어 (일반 사용자)

### 사용법
```
!제작가능 [레시피]
```

### 규칙
- 해당 레시피를 현재 재고로 만들 수 있는지 확인한다.
- 봇은 레시피 DB에서 필요한 재료 구성을 조회한 뒤, 메인 재고 DB와 비교한다.
- **출력 결과**:
  - **제작 가능**: 모든 재료의 상태가 `이상없음`인 경우.
  - **제작 가능하나 재료 부족 가능**: 재료 중 `위험` 또는 `부족` 상태가 포함된 경우 (어떤 재료가 부족할 수 있는지 함께 안내).
  - **제작 불가**: 재료 중 `소진` 또는 `만료` 상태가 포함된 경우 (어떤 재료가 문제인지 함께 안내).
- 레시피가 DB에 없으면 오타 확인 메시지.

---

## 10. `!정기활동` 명령어 (일반 사용자, 일부 관리자 전용)

동아리에서 매주 모여서 같은 주제의 칵테일을 다 같이 만드는 시간을 위한 명령어.

### `!사용 레시피`와의 차이
- `!사용 레시피`는 **개인이 즉시 사용**하는 명령어. 입력 즉시 재고가 차감된다.
- `!정기활동`은 **부장이 정기활동에 사용할 레시피를 미리 등록**해두는 명령어. 등록만으로는 재고가 차감되지 않고, `!정기활동 시작`을 쳤을 때 비로소 최종 차감이 일어난다.

### 사용법
```
!정기활동 [레시피] [제작 수량], [레시피] [제작 수량], ...   ← 금주 정기활동 등록 (재고 차감 X)
!정기활동 시작                                             ← 정기활동 시작 + 재고 최종 차감
!정기활동 종료                                             ← 정기활동 종료 표시
```

### 규칙

#### (1) `!정기활동 [레시피] [제작 수량], ...` (등록)
- 부장이 단톡방에 명령어를 입력해서 금주 정기활동에 만들 칵테일들을 미리 등록한다.
- **이 단계에서는 재고를 차감하지 않는다**. 단지 레시피와 제작 수량을 정기활동 DB에 저장만 한다.
- 봇은 등록된 레시피들의 재료 구성을 계산해 **현재 재고로 만들 수 있는지 미리 점검**한다.
  - 차감 시뮬레이션 결과 **`위험`/`부족`/`소진`/`만료` 상태가 되는 재료가 있으면 경고 메시지**를 함께 출력 ("○○ 재료가 부족할 수 있습니다" 등).
- 응답 메시지는 명령어를 친 부장에게 출력되며, 부장이 단톡방에 공유하는 형태로 동방 인원에게 알린다.
- 새 등록 명령어를 다시 치면 이전 등록 내용을 덮어쓴다.

#### (2) `!정기활동 시작`
- 등록된 정기활동 레시피들의 재료를 **실제로 메인 재고 DB에서 차감**한다 (`!사용 레시피`와 동일한 차감 로직).
- 정기활동 진행 상태를 "진행 중"으로 표시한다.
- 이후 `!동방` 명령어 실행 시 "정기활동 중" 상태와 등록된 칵테일 정보가 함께 출력된다.

#### (3) `!정기활동 종료`
- 정기활동 진행 상태를 해제한다 ("진행 중" 표시 off).
- 등록된 정기활동 내용은 그대로 유지되며, 다음 `!정기활동` 등록 명령어로 덮어쓰일 때까지 보존된다.

---

## 11. `!동방` 명령어 (일반 사용자)

### 사용법
```
!동방
```

### 규칙
- 현재 동아리방을 사용 중인 인원 정보를 출력한다.
- **출력 내용**:
  - 현재 사용 인원의 이름 목록과 총 인원수.
  - 정기활동 진행 중이면 "정기활동 중" 표시와 함께 오늘 만드는 칵테일 목록 출력.
- **사용 인원 추가 경로**:
  - `!사용` 명령어 입력 시 자동 추가.
  - `!입장` 명령어로 직접 추가.

---

## 12. `!입장` 명령어 (일반 사용자)

### 사용법
```
!입장                              ← 본인만 입장
!입장 [이름], [이름], ...          ← 본인 + 동반자 입장
```

### 규칙
- 칵테일을 만들지 않고 동방에 들어와 있을 때 사용.
- **본인은 항상 자동 포함**된다 (동방에 없는 사람이 입장 명령어를 치는 것은 부자연스러움).
- 추가 이름을 적으면 여러 명을 한 번에 입장 처리할 수 있다.
- 부장이 다른 부원들을 한 번에 입장 처리할 때도 활용 (예: 부장이 `!입장 김철수, 이영희`를 치면 부장 본인 + 김철수 + 이영희 3명 추가).

---

## 13. `!완료` 명령어 (일반 사용자)

### 사용법
```
!완료
```

### 규칙
- 칵테일을 다 마시고 설거지를 마친 뒤 동방에서 나갈 때 사용.
- 현재 동방 사용 인원 목록에서 **본인을 제거**한다.
- **로그 DB에 나간 시간을 기록**한다.
- **정기활동과 독립적**: 정기활동 진행 중에도 `!완료`는 단순히 본인의 퇴장만 처리한다. 마지막 사람이 `!완료`를 쳐도 정기활동 진행 상태는 자동으로 해제되지 않는다. 정기활동 종료는 반드시 `!정기활동 종료` 명령어로만 처리한다.

---

## 14. `!동방인원수정` 명령어 (⚠️ 관리자 전용)

### 사용법
```
!동방인원수정 추가 [이름]
!동방인원수정 제거 [이름]
```

### 규칙
- 실수로 `!완료`를 치지 않고 나간 인원을 제거하거나, 누락된 인원을 임의로 추가할 때 사용.
- 관리자 전용.

---

## 로그 DB

동방 인원의 출입과 재료 사용 기록을 저장한다.

### 컬럼 구조 (예시)

| 일시 | 사용자 | 동작 | 상세 |
|------|-------|------|------|

- **동작**: 입장 / 완료(퇴장) / 사용(재료) / 사용(레시피) 등.
- **상세**: 사용한 재료와 양, 또는 만든 레시피와 수량 등.

### 용도
- 어느 부원이 언제 왔다 갔는지 확인.
- 어느 재료를 누가 얼마나 소비했는지 확인.
- 분쟁 시 근거 자료, 소비 패턴 분석.

---

## 3-1. 레시피 DB

칵테일 레시피, `oz` 기준으로 작성.

### 컬럼 구조

| 이름 | 레시피 | 특징 |
|------|-------|------|

### 규칙
- 레시피는 직접 수정한다.

---

## 정리: 명령어 한눈에 보기

| 명령어 | 권한 | 용도 | 입력 단위 | DB 저장 단위 |
|--------|------|------|----------|------------|
| `!사용 재료 ...` | 일반 | 사용한 재료/양 기록 및 DB 차감 | oz (많을 시 mL) | oz |
| `!사용 레시피 ...` | 일반 | 만든 칵테일 기록 및 재료 자동 차감 | 잔 수 | oz |
| `!소진` | 일반 | 재료(들)를 소진 상태로 변경 | - | - |
| `!추천` | 일반 | 남은 재고 기반 칵테일 추천 | - | - |
| `!취소` | 일반 | 본인의 마지막 명령 되돌리기 | - | - |
| `!도움말` | 일반 | 명령어 사용법 안내 | - | - |
| `!재료` | 일반 | 특정 재료의 상태 확인 | - | - |
| `!제작가능` | 일반 | 특정 레시피의 제작 가능 여부 확인 | - | - |
| `!정기활동` | 일반(등록은 부장) | 정기활동 레시피 미리 등록(저장만), `시작` 시 최종 차감, `종료`로 해제 | 잔 수 | oz |
| `!동방` | 일반 | 현재 동방 사용 인원 및 정기활동 상태 조회 | - | - |
| `!입장` | 일반 | 본인 + 동반자 동방 사용 인원에 추가 | - | - |
| `!완료` | 일반 | 본인을 동방 사용 인원에서 제거, 로그 기록 | - | - |
| `!추가` | 관리자 | 재고 보충 | mL | oz (환산 저장) |
| `!재고` | 관리자 | 이상 있는 재고를 상태별로 정리해 출력 | - | - |
| `!동방인원수정` | 관리자 | 사용 인원 임의 추가/제거 | - | - |

## 참고할 DB
- **메인 재고 DB**: 물품 이름 / 병 개수 / 남은 양(oz) / 상태
- **병 상세 DB**: 물품 이름 / 병 번호 / 용량(oz) / 소비기한
- **레시피 DB**: 이름 / 레시피(oz) / 특징
- **명령 히스토리** (`!취소` 구현용): 사용자 / 명령어 / 입력값 / 실행 시각 / (롤백에 필요한 이전 상태)
- **동방 사용 인원**: 현재 동방에 있는 사람들의 목록 (이름 / 입장 시각).
- **정기활동 상태**: 진행 여부(bool) / 등록된 레시피 목록 / 등록 시각.
- **로그 DB**: 일시 / 사용자 / 동작 (입장/완료/사용 등) / 상세.

---

## 추후 결정 필요 항목 (TODO)
- [ ] **레시피 DB 정립** (구체적인 항목 구조와 칵테일 데이터 정리)
  - 이후 `!추천` 옵션 지정 가능 여부 검토 (예: `!추천 단맛`)
- [ ] `!취소` 가능 시간 제한 (예: 1시간 이내)
- [ ] 관리자 권한 식별 방법 (카톡 ID 기반 등)
- [ ] `!정기활동 [레시피] ...` 등록 명령어의 권한 (부장 전용으로 제한할지)
- [ ] 사용자 이름 식별 방법 (카톡 닉네임 기반 / 별도 등록 필요 여부)
- [ ] `!완료` 시 동방에 본인이 없으면 어떻게 처리할지 (오류? 무시?)
